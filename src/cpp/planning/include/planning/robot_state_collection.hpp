#include "planning/robot_state.hpp"


class RobotStateCollection {

    public:
        // add_edge(RobotState*, RobotState*)
        // std::vector<RobotState*> get_states();
        // RobotState* nearest(RobotState);
        // void insert();
        // int size();
    private:

};